@model RPImobiliaria.Models.EstadoImovel
@{ ViewData["Title"] = Model.Nome; }
<div class="container py-4"><a asp-action="Index" class="btn btn-link px-0 mb-3"><i class="bi bi-arrow-left"></i> Estados</a><div class="card border-0 shadow-sm"><div class="card-body p-4"><p class="text-uppercase text-body-secondary small fw-bold">Estado do imóvel</p><h1 class="h3 fw-bold mb-4">@Model.Nome</h1><a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">Editar</a></div></div></div>
